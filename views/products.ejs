<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Data+AI</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" rel="stylesheet">
  <style>
    :root {
      --primary-color: #b3daa0;
      --secondary-color: darkseagreen;
      --text-color: #333333;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: var(--text-color);
    }
    .hero-section {
      background: linear-gradient(rgba(179, 218, 160, 0.7), rgba(143, 188, 143, 0.7)), url('/api/placeholder/1920/1080');
      background-size: cover;
      background-position: center;
      color: var(--text-color);
      padding: 120px 0;
      position: relative;
      overflow: hidden;
    }
    .hero-section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('/api/placeholder/1920/1080') no-repeat center center;
      background-size: cover;
      filter: blur(5px);
      z-index: -1;
    }
    .feature-card {
      border: none;
      transition: transform 0.3s, box-shadow 0.3s;
      border-radius: 10px;
      overflow: hidden;
      background-color: #ffffff;
    }
    .feature-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.1);
    }
    .icon-circle {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      background-color: var(--primary-color);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
      transition: transform 0.3s ease;
    }
    .feature-card:hover .icon-circle {
      transform: scale(1.1);
    }
    .icon-circle i {
      font-size: 30px;
      color: var(--text-color);
    }
    .btn-primary {
      background-color: var(--secondary-color);
      border-color: var(--secondary-color);
      color: var(--text-color);
      padding: 10px 25px;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background-color: var(--primary-color);
      border-color: var(--primary-color);
      transform: translateY(-3px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    .section-title {
      position: relative;
      padding-bottom: 15px;
      margin-bottom: 30px;
      color: var(--text-color);
    }
    .section-title::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 50px;
      height: 3px;
      background-color: var(--secondary-color);
    }
    .how-it-works {
      background-color: var(--primary-color);
      position: relative;
      overflow: hidden;
    }
    .how-it-works::before {
      content: '';
      position: absolute;
      top: -50px;
      left: -50px;
      right: -50px;
      bottom: -50px;
      background: url('/api/placeholder/500/500') repeat;
      opacity: 0.1;
      transform: rotate(45deg);
    }
    .how-it-works img {
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      border-radius: 10px;
    }
    .cta-section {
      background: linear-gradient(45deg, var(--primary-color), var(--secondary-color));
    }
    .text-primary {
      color: var(--secondary-color) !important;
    }
    .data-fabric-concept {
      border: 2px solid var(--secondary-color);
      border-radius: 10px;
      padding: 20px;
      background-color: rgba(255, 255, 255, 0.8);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .data-fabric-concept:hover {
      transform: translateY(-5px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }
    .animate-on-scroll {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.5s ease, transform 0.5s ease;
    }
    .animate-on-scroll.visible {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>
<%- include('_header') %>
<body>
<header>
  <%- include('_menu') %>
</header>

<main role="main">
  <section class="hero-section text-center">
    <div class="container">
      <h1 class="display-4 font-weight-bold mb-4 animate-on-scroll">AI-Powered Data Fabric Solutions</h1>
      <p class="lead mb-5 animate-on-scroll">Unify Your Data Landscape with Intelligent, Automated Data Management</p>
      <a href="#" class="btn btn-primary btn-lg animate-on-scroll">Explore Solutions</a>
    </div>
  </section>

  <section class="py-5">
    <div class="container">
      <h2 class="text-center section-title animate-on-scroll">Key Features of Our AI-Powered Data Fabric</h2>
      <div class="row">
        <div class="col-md-4 mb-4">
          <div class="card feature-card h-100 animate-on-scroll">
            <div class="card-body text-center">
              <div class="icon-circle">
                <i class="fas fa-brain"></i>
              </div>
              <h5 class="card-title">AI-Driven Integration</h5>
              <p class="card-text">Leverage machine learning for intelligent data integration across diverse sources.</p>
            </div>
          </div>
        </div>
        <div class="col-md-4 mb-4">
          <div class="card feature-card h-100 animate-on-scroll">
            <div class="card-body text-center">
              <div class="icon-circle">
                <i class="fas fa-project-diagram"></i>
              </div>
              <h5 class="card-title">Automated Data Discovery</h5>
              <p class="card-text">Automatically identify and catalog data assets across your entire ecosystem.</p>
            </div>
          </div>
        </div>
        <div class="col-md-4 mb-4">
          <div class="card feature-card h-100 animate-on-scroll">
            <div class="card-body text-center">
              <div class="icon-circle">
                <i class="fas fa-shield-alt"></i>
              </div>
              <h5 class="card-title">Intelligent Data Governance</h5>
              <p class="card-text">Ensure compliance and security with AI-powered data governance tools.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-5 how-it-works">
    <div class="container">
      <h2 class="text-center section-title animate-on-scroll">How Our AI-Powered Data Fabric Works</h2>
      <div class="row align-items-center">
        <div class="col-md-6 mb-4 mb-md-0">
          <div class="data-fabric-concept animate-on-scroll">
            <h4 class="mb-3">Data Fabric Concept</h4>
            <p>Imagine a seamless, intelligent network that connects and manages all your data sources, applications, and analytics tools. Our AI-powered Data Fabric acts as this unifying layer, providing:</p>
            <ul>
              <li>Real-time data integration and synchronization</li>
              <li>Automated data discovery and cataloging</li>
              <li>Intelligent data governance and security</li>
              <li>Seamless data access and delivery across your organization</li>
            </ul>
            <p>This unified approach enables faster insights, better decision-making, and enhanced data utilization across your enterprise.</p>
          </div>
        </div>
        <div class="col-md-6">
          <ol class="list-unstyled">
            <li class="mb-3 animate-on-scroll"><strong class="text-primary">1. Connect:</strong> Seamlessly integrate all your data sources.</li>
            <li class="mb-3 animate-on-scroll"><strong class="text-primary">2. Discover:</strong> AI automatically catalogs and classifies your data.</li>
            <li class="mb-3 animate-on-scroll"><strong class="text-primary">3. Transform:</strong> Cleanse and enrich data with machine learning algorithms.</li>
            <li class="mb-3 animate-on-scroll"><strong class="text-primary">4. Govern:</strong> Ensure compliance with intelligent data governance.</li>
            <li class="animate-on-scroll"><strong class="text-primary">5. Deliver:</strong> Provide unified, high-quality data to all your applications.</li>
          </ol>
        </div>
      </div>
    </div>
  </section>
</main>

<footer>
  <%- include('_footer')%>
</footer>

<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
<script>
  $(document).ready(function() {
    // Smooth scroll
    $('a[href^="#"]').on('click', function(event) {
      var target = $(this.getAttribute('href'));
      if (target.length) {
        event.preventDefault();
        $('html, body').stop().animate({
          scrollTop: target.offset().top - 100
        }, 1000);
      }
    });

    // Animate on scroll
    function animateOnScroll() {
      $('.animate-on-scroll').each(function() {
        var elementBottom = $(this).offset().top + $(this).outerHeight();
        var windowBottom = $(window).scrollTop() + $(window).height();
        if (elementBottom < windowBottom) {
          $(this).addClass('visible');
        }
      });
    }

    $(window).on('scroll', animateOnScroll);
    animateOnScroll();

    // Parallax effect for hero section
    $(window).scroll(function() {
      var scrollTop = $(this).scrollTop();
      $('.hero-section').css('background-position', 'center ' + (scrollTop * 0.5) + 'px');
    });

    // Hover effect for feature cards
    $('.feature-card').hover(
            function() {
              $(this).find('.icon-circle').css('transform', 'scale(1.1) rotate(10deg)');
            },
            function() {
              $(this).find('.icon-circle').css('transform', 'scale(1) rotate(0deg)');
            }
    );
  });
</script>
</body>
</html>